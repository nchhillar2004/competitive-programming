#include <bits/stdc++.h>
using namespace std;

// Unordered set - does NOT store in sorted order, every element is unique
// time complexity - mostly O(1), worst case: O(n)
int main() {
    // everything same as set
    unordered_set<int> us;
    // {1, 3, 2, 4} can be in random order, But should be UNIQUE
    // all functions same set, except upper_bound and lower_bound
}
